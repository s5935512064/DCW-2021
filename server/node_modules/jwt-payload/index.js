const jwt = require('jwt-simple');

module.exports = function token(header, secret, alg) {
    
    let headers = header.authorization;
    let token = headers.slice(7);
    let decoded = jwt.decode(token, secret, false, alg);
    return paylod = decoded;

};  
